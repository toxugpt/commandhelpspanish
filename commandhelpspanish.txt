sudo adduser usuario

sudo adduser usuario --ingroup grupo

su - usuario

id usuario

sudo usermod -g grupo usuario

sudo usermod -aG grupo usuario

sudo deluser usuario

sudo deluser --remove-home usuario

sudo addgroup grupo

sudo groupdel grupo

cat /etc/group

groups usuario

sudo passwd usuario

Cambiar tu contraseña
passwd

Cambiar usuario y grupo:
sudo chown usuario:grupo archivo

Cambiar solo grupo:
sudo chgrp grupo archivo


adduser
addgroup
usermod
deluser
groupdel
su -
id
groups
chown
